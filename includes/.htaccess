# Block all direct access to this folder. These files are loaded via
# PHP's require/include, never requested directly by a browser.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
